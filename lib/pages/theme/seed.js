const setThemeCode = `import useThemeDefault from '@/composables/useThemeDefault'

const { setTheme } = useThemeDefault()
setTheme({ fontSize: 'text-[20px]', select_optionFontSize: 'text-[16px]' ... })`

export default { setThemeCode }
